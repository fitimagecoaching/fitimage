[phases.setup]
nixPkgs = ["nodejs_20", "python3", "gcc", "gnumake"]

[phases.install]
cmds = ["npm install", "npm rebuild better-sqlite3"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"

[variables]
PORT = "8080"
